name: cancel_annotation_research
version: 2.0.0
description: Cancel an annotation research workflow while retaining a durable task audit record.
category: annotation
subcategory: autonomous_curation
execution:
  type: client-side
  requires_data: true
parameters:
  type: object
  properties:
    taskId: { type: string }
    clientId: { type: string }
  required: [taskId]
metadata:
  added_in: autonomous_annotation_v2
  priority: medium
  tags: [annotation, research, dgr, cancel]
