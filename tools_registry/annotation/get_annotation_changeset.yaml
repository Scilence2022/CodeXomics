name: get_annotation_changeset
version: 2.0.0
description: Retrieve an annotation ChangeSet and its validation, approval, or commit state.
category: annotation
subcategory: autonomous_curation
execution:
  type: client-side
  requires_data: true
parameters:
  type: object
  properties:
    changeSetId:
      type: string
    clientId:
      type: string
  required: [changeSetId]
metadata:
  added_in: autonomous_annotation_v2
  priority: high
  tags: [annotation, changeset, review]
