name: get_annotation_research_workflow
version: 2.0.0
description: Retrieve a resumable DGR research workflow and create a reviewable ChangeSet when research completes.
category: annotation
subcategory: autonomous_curation
execution:
  type: client-side
  requires_data: true
parameters:
  type: object
  properties:
    taskId: { type: string }
    clientId: { type: string }
  required: [taskId]
metadata:
  added_in: autonomous_annotation_v2
  priority: high
  tags: [annotation, research, dgr, workflow]
